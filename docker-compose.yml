services:
  notepad:
    build: .
    env_file:
      - /opt/online-notepad/.env
    ports:
      - "31300:3000"
    environment:
      DATA_DIR: /data
    volumes:
      - notepad-data:/data
    restart: unless-stopped

volumes:
  notepad-data:
